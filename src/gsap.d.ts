declare module 'gsap/dist/ScrollTrigger' {
    const ScrollTrigger;
    export default ScrollTrigger;
  }
  export interface AnimationProps {
    duration: number; // Assuming duration is a number
    ease: string; // Assuming ease is a string
  }